# To join 2 tuples together, use the + operator
a = ("Apple",)
b = ("Banana", "Cherry")
c = a + b
print("Joining tuple a and tuple b together with the + operator")
print(c)

# To duplicate the content of a tuple, use the * operator
print("Duplicating the content of tuple c with the * operator")
print(c * 2)